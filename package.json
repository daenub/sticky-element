{
  "name": "sticky-element",
  "version": "0.1.0",
  "description": "Sticky elements",
  "main": "dist/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --presets babel-preset-env --out-dir dist",
    "newBuild": "babel src --presets env --out-dir dist"
  },
  "author": "Dan Büschlen @daenub",
  "license": "MIT",
  "dependencies": {
    "babel-plugin-lodash": "^3.3.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  }
}
